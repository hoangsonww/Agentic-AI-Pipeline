apiVersion: v1
kind: Service
metadata:
  name: agentic-ai-stable
  labels:
    app: agentic-ai
    track: stable
spec:
  type: ClusterIP
  selector:
    app: agentic-ai
    track: stable
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
