# direnv: load project env automatically (requires `direnv allow`)
export MODEL_PROVIDER=${MODEL_PROVIDER:=openai}
export OPENAI_API_KEY=${OPENAI_API_KEY:=set-me}
export CHROMA_DIR=${CHROMA_DIR:=.chroma}
export SQLITE_PATH=${SQLITE_PATH:=.sqlite/agent.db}
export APP_HOST=${APP_HOST:=0.0.0.0}
export APP_PORT=${APP_PORT:=8000}
layout python python3
